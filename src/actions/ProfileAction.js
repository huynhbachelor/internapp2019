export const imgChange = (img) => {
    return {
        type: 'IMGCHANG',
        img
    };
}

const profileChange = (profile) => {
    return {
        type: 'ISCHANGE',
        profile
    };
};


export default profileChange;
